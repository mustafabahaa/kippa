import type { KippaModeTokens } from '../../foundations/mode';
import type { KippaThemeMode } from '../../foundations/types';

export type OverrideContext = { mode: KippaThemeMode; tokens: KippaModeTokens };
